{
  "name": "4444555-arcade",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "dev": "vite apps/portal --config apps/portal/vite.config.ts",
    "build": "npm run build --workspace @4444555/portal",
    "typecheck": "tsc --noEmit -p packages/game-sdk/tsconfig.json && tsc --noEmit -p apps/portal/tsconfig.json",
    "test": "node --test tests/*.test.mjs && node --experimental-strip-types --test tests/*.test.ts",
    "verify": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": { "typescript": "5.9.3" }
}
