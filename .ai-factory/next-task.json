{
  "task_id": "ARCADE-GODOT-PILOT-GAME-001",
  "candidate_sha": "e0eebb0ec5dcf9670510848c47484ca4dc97c734",
  "pr_number": 31,
  "allow_worker_edits": true,
  "prompt": "Eighth correction for PR #31. Start from exact head e0eebb0ec5dcf9670510848c47484ca4dc97c734 on phase/godot-pilot-pulse-loom. Read AGENTS.md, docs/WORKFLOW.md, docs/OWNER_PREVIEW_GATE.md, .ai-factory/PILOT.md, the current PR comments, and the Pulse Loom implementation/tests. Do not modify main or factory/control and do not open another PR. Current Verify #94 is green, and the runtime assisted-pulse source of truth plus Square/Circle lane/color mapping are now correct; preserve them. Close only the remaining seventh-correction acceptance gaps. (1) In tests/pulse-loom.test.mjs, remove the independent hard-coded assisted route sequence 0->2, 2->3, 5->4 from the Node onboarding model. The Node test may model generic three-step onboarding/clock/scoring behavior, but it must not duplicate the authoritative runtime source/target table. Exact assisted pulse identities must be verified only through Godot headless tests reading PulseLoomConstants.get_assisted_pulse_spec(). (2) Add a deterministic Godot headless stale-preview transition test that uses at least two simultaneous active pulses: pulse A is nearest and both signal_core and radar_lanes must preview/highlight A; after A is resolved or missed and removed, pulse B becomes nearest and both preview/highlight B; after B is removed, both preview states clear fully (source and target = -1); then add a new repeated Emerald Square or Crimson Circle pulse from a different source and prove source/target, required rotor step, routed preview lane, alignment, signal_core target, radar_lanes target, and actual resolution refresh correctly with no stale state. Keep the existing repeated-occurrence exhaustive coverage for all six target lanes and exact Square □ Emerald Color(0.0,1.0,0.6) / Circle ○ Crimson Color(1.0,0.20,0.4) assertions. Change runtime code only if the new behavioral stale-preview test exposes a real bug. Preserve gameplay, onboarding clock hold/retry behavior, route visualization, scoring, SDK/ticket/message security, pause lifecycle, portal integration, and single-threaded Web export policy. Run npm run verify, the Godot 4.6.3 headless smoke test, a clean single-threaded Web export, production and /test/ builds with all four non-empty Godot artifacts, and clean git-status/ignore checks. Commit and push the correction to phase/godot-pilot-pulse-loom, then write .ai-factory/result.json with verification evidence and the pushed HEAD."
}
