{
  "task_id": "ARCADE-GODOT-PILOT-GAME-001",
  "candidate_sha": "f694efce4c0e9df8374a1248cfe9c7d7c6954432",
  "pr_number": 0,
  "allow_worker_edits": true,
  "prompt": "Second correction pass for ARCADE-GODOT-PILOT-GAME-001 in web688/4444555.xyz. Read .ai-factory/session.json, AGENTS.md, docs/WORKFLOW.md, docs/PROJECT_STATE.md, docs/ROADMAP.md Phase 3 technical contract, README.md, docs/GAME_SDK.md, docs/CONTRIBUTING.md, docs/ARCHITECTURE.md, docs/OWNER_PREVIEW_GATE.md, and .ai-factory/PILOT.md. Work only on existing branch phase/godot-pilot-pulse-loom from exact current head f694efce4c0e9df8374a1248cfe9c7d7c6954432. Do not change main or factory/control, do not touch Hullwatch/PR #29, do not open or merge a PR, and do not ask the owner routine questions. Preserve Pulse Loom gameplay/visual rules unless a narrow integration fix requires otherwise.\n\nController re-review found the first correction improved export wiring, watchdog error surfacing, SDK imports, and Godot-side origin checking, but acceptance still FAILS. Fix these concrete blockers:\n1) Stable host/ticket lifecycle: PulseLoomGate currently recreates createPulseLoomHost whenever muted/onExit changes, which discards currentTicket. Keep one host for the mounted game; changing mute/reduced-motion must update settings without replacing the active host/ticket. Add behavioral coverage proving a ticket remains valid across a settings/mute update.\n2) Enforce the Game SDK run-ticket contract end to end. A run must not start without a matching, unexpired host-issued RunTicket. Remove direct READY start/retry bypasses from Godot keyboard/touch paths (GameManager/UIOverlay currently call start_run/reset paths without a host ticket). Validate required ticket fields, gameId=pulse-loom, gameVersion=0.1.0, ruleset=conduit-v1, and expiry before START/RESTART. RUN_ENDED must be rejected unless result.ticketId exactly matches the active ticket; do not substitute the active ticket ID for a mismatched result. validateScoreClaim must reject expired tickets. For this local anonymous host use the repository's explicit local semantics signature='local-unverified'; do not fabricate a signature that looks signed.\n3) Exact same-origin messaging with no wildcard/opaque-origin bypass. sendPostMessageToGodot must never fall back to '*'. Parent validation must require event.source===iframe.contentWindow AND event.origin===the exact expected portal origin, including tests that actually execute origin mismatch/empty/null rejection under Node rather than skipping origin checks because global window is absent. Godot iframe listener must also require event.source===window.parent and event.origin===window.location.origin.\n4) Strict payload validation. Do not coerce malformed payloads with String()/Number() defaults. Reject arrays, missing/empty identifiers, wrong GAME_READY game/version/sdk, invalid states/outcomes/medals, non-finite/non-number telemetry or score fields, negative/out-of-range values, and malformed RUN_ENDED data. Add behavioral tests for representative malformed and cross-origin cases.\n5) Lifecycle visibility: docs/GAME_SDK.md requires pause on visibility loss. Add portal visibility handling so a running run pauses when hidden and does not spuriously create/replace a ticket. Preserve deterministic pause/resume.\n6) Keep the already-correct export/preview wiring and watchdog. Do not reintroduce source-substring regex tests. Ensure the exporter/fallback cannot report success for a partial artifact set: index.html, index.js, index.wasm, and index.pck are all required.\n\nVerification required before PASS: npm run verify; explicit Godot headless smoke including ticket rejection/acceptance behavior; clean single-threaded Godot 4.6.3 Web export after deleting generated Pulse Loom output; verify all four artifacts are non-empty; production dist contains all four; build the portal with --base=/test/ and prove its dist contains all four under games/pulse-loom; behavioral tests cover host ticket persistence, expiry/mismatch, exact-origin rejection, malformed payload rejection, and no hostless start; generated exports and .ai-factory remain untracked. Commit and push the correction to phase/godot-pilot-pulse-loom. Before exit write .ai-factory/result.json with exact commands, outcomes, artifact paths/sizes, git status evidence, and pushed HEAD. Otherwise report FAIL, BLOCKED, or NEEDS_CONTROLLER."
}
