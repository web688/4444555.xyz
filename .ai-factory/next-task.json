{
  "task_id": "ARCADE-GODOT-PILOT-GAME-001",
  "candidate_sha": "b7da0b4ccf82e6b79fbf00bd2f7ffd54c82360f6",
  "pr_number": 0,
  "allow_worker_edits": true,
  "prompt": "Fourth correction pass for ARCADE-GODOT-PILOT-GAME-001 in web688/4444555.xyz. Read .ai-factory/session.json, AGENTS.md, docs/WORKFLOW.md, docs/PROJECT_STATE.md, docs/ROADMAP.md Phase 3, README.md, docs/GAME_SDK.md, docs/CONTRIBUTING.md, docs/ARCHITECTURE.md, docs/OWNER_PREVIEW_GATE.md, and .ai-factory/PILOT.md. Work only on existing branch phase/godot-pilot-pulse-loom from exact current head b7da0b4ccf82e6b79fbf00bd2f7ffd54c82360f6. Do not change main or factory/control, do not touch Hullwatch/PR #29, do not open or merge a PR, and do not ask the owner routine questions. Preserve the accepted Pulse Loom gameplay/visual behavior and the already-correct SDK lifecycle, same-origin/source checks, visibility pause, strict ticket fields/types, deterministic daily seed semantics, host ticket persistence, generated-export ignore policy, and portal launch-button behavior.\n\nController acceptance still FAILS narrowly:\n\n1) Enforce the actual host timestamp wire format, not a loose datetime parse. createPulseLoomRunTicket() emits JavaScript Date.toISOString(), i.e. UTC timestamps shaped exactly like YYYY-MM-DDTHH:mm:ss.sssZ. game_manager.gd parse_iso_datetime currently strip_edges(), accepts missing Z/lowercase z, accepts arbitrary fractional precision, and therefore accepts strings the host never emits. Replace this with strict validation of the host wire form before conversion. Reject leading/trailing whitespace, missing Z, lowercase z, timezone offsets, missing fractional milliseconds, fractional precision other than exactly 3 digits, malformed separators, and impossible calendar/time values. Keep issuedAt/expiresAt required strings; require expiresAt > issuedAt and expiresAt strictly in the future.\n\n2) Extend the Godot headless negative suite so the strict wire contract is executable. At minimum reject: no Z, lowercase z, +00:00/+offset form, no fractional part, 1/2/4+ fractional digits, leading/trailing whitespace, malformed separators, impossible date such as 2026-02-30, and impossible time such as 24:00:00.000Z. Prove these malformed tickets cannot move READY -> RUNNING through either START or RESTART. Keep positive tests using the exact Date.toISOString() shape with .sssZ.\n\n3) Remove the remaining broad coercion at the raw Godot callback boundary. web_bridge.gd _on_js_message_raw currently does str(raw). The JavaScript listener deliberately calls the Godot callback with JSON.stringify(payload), so require TYPE_STRING and reject any non-string callback value instead of coercing it. Add a headless/unit-level check if practical without broadening scope; at minimum keep handle_command's existing strict type checks.\n\n4) Make the deterministic daily-seed Node coverage clock-stable. The current test creates tickets and then independently calls getPulseLoomDailySeed(), which can flake if UTC midnight occurs between calls. Prove same-day determinism without a midnight race (for example by deriving expected day from issuedAt or by a narrow injectable clock helper), while preserving unique ticket IDs and explicit seed override behavior. Do not weaken uniqueness or schema assertions.\n\n5) Do not try to solve /test deployment bootstrap in this local pass. The controller will handle the workflow_run/default-branch bootstrap separately after this candidate is accepted. Preserve existing workflow changes unless verification requires a narrow fix.\n\nVerification required before PASS: npm run verify; explicit Godot headless smoke with the new strict timestamp rejection matrix and START/RESTART non-transition proof; clean single-threaded Godot 4.6.3 Web export after deleting generated Pulse Loom output; verify index.html/index.js/index.wasm/index.pck are all non-empty; verify production dist contains all four; build portal with --base=/test/ and prove dist contains all four under games/pulse-loom; run behavioral Node tests including daily seed, ticket persistence/expiry/mismatch, exact-origin/source, malformed payloads; git status evidence showing generated exports and .ai-factory remain untracked/ignored. Commit and push the correction to phase/godot-pilot-pulse-loom. Before exit write .ai-factory/result.json with exact commands/outcomes, artifact paths/sizes, git status evidence, and pushed HEAD. Otherwise report FAIL, BLOCKED, or NEEDS_CONTROLLER."
}
