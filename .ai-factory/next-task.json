{
  "task_id": "ARCADE-GODOT-PILOT-GAME-001",
  "candidate_sha": "4c3c7cc9a666ca9c4ceacb7419c70271f44ca17c",
  "pr_number": 31,
  "allow_worker_edits": true,
  "prompt": "Seventh correction for PR #31. Start from exact head 4c3c7cc9a666ca9c4ceacb7419c70271f44ca17c on phase/godot-pilot-pulse-loom. Read the repository instructions, factory PILOT context, current PR comments, and current Pulse Loom implementation. Do not modify main or factory/control and do not open another PR. Current CI is green, but the previous correction is incomplete. Keep the canonical mapping: lane 3 = SQUARE □ = Emerald Color(0.0,1.0,0.6); lane 4 = CIRCLE ○ = Crimson Color(1.0,0.20,0.4). Create one authoritative runtime assisted-pulse definition/helper for all three guided steps and make _spawn_assisted_pulse consume it. Keep the sequence 0->2, 2->3, 5->4 with current speeds. Names, symbols, and colors must be derived from PulseLoomConstants using target_lane. Godot headless onboarding tests must query the same runtime helper instead of duplicating the table; simplify the Node onboarding model so it does not independently duplicate the exact source/target sequence. Strengthen the headless repeated-target test so every target lane is exercised at least twice from different sources and proves pulse target, signal_core target, radar_lanes target, required step, routed preview, alignment, and actual resolution all agree. Add explicit Square name/symbol/exact Emerald-color and Circle name/symbol/exact Crimson-color assertions. Add a deterministic stale-preview test: pulse A nearest -> preview/highlight A; after A resolves or misses and is removed, pulse B becomes nearest -> preview/highlight B; no pulse -> both clear; then a new repeated Square or Circle from another source -> preview/highlight/routing refresh with no stale state. Fix runtime code only if that behavioral test exposes a bug. Preserve all current gameplay, onboarding clock hold/retry behavior, routing visualization, scoring, SDK/ticket/message security, pause lifecycle, portal integration, and export policy. Run npm run verify, Godot 4.6.3 headless smoke, clean single-threaded Web export, production and /test/ builds with all four non-empty Godot artifacts, and clean git-status/ignore checks. Commit and push the correction, then write .ai-factory/result.json with verification evidence and pushed HEAD."
}