{
  "task_id": "ARCADE-GODOT-PILOT-GAME-001",
  "candidate_sha": "7cbda778f8c85850721aebae17f9e48b20ab0497",
  "pr_number": 0,
  "allow_worker_edits": true,
  "prompt": "Third correction pass for ARCADE-GODOT-PILOT-GAME-001 in web688/4444555.xyz. Read .ai-factory/session.json, AGENTS.md, docs/WORKFLOW.md, docs/PROJECT_STATE.md, docs/ROADMAP.md Phase 3 technical contract, README.md, docs/GAME_SDK.md, docs/CONTRIBUTING.md, docs/ARCHITECTURE.md, docs/OWNER_PREVIEW_GATE.md, and .ai-factory/PILOT.md. Work only on existing branch phase/godot-pilot-pulse-loom from exact current head 7cbda778f8c85850721aebae17f9e48b20ab0497. Do not change main or factory/control, do not touch Hullwatch/PR #29, do not open or merge a PR, and do not ask the owner routine questions. Preserve the already-correct host lifecycle, same-origin messaging, strict inbound validation, visibility pause, watchdog, export wiring, and Pulse Loom gameplay/visual rules except for the narrow corrections below.\n\nController review of the live GitHub HEAD found acceptance still FAILS on the same logical task because the Godot side does not enforce the complete shared RunTicket schema:\n1) Full RunTicket schema before START/RESTART. games/pulse-loom/scripts/game_manager.gd currently accepts a ticket without issuedAt and silently defaults a missing seed to \"1337\". Require all SDK RunTicket fields with their real types: non-empty string id, gameId, gameVersion, issuedAt, expiresAt, seed, ruleset, signature. Require id prefix run-pl-, gameId=pulse-loom, gameVersion=0.1.0, ruleset=conduit-v1, signature=local-unverified. Reject missing/wrong-type/empty fields rather than coercing them with str()/int() fallbacks. Validate issuedAt and expiresAt as real host-format ISO timestamps, require expiresAt > issuedAt and expiresAt strictly in the future. Do not invent a seed when it is absent.\n2) Make the headless test prove the real contract. get_valid_test_ticket() currently omits issuedAt, so the smoke suite blesses a malformed ticket. Build valid test tickets with the same ISO-8601 shape emitted by JavaScript Date.toISOString() (including fractional seconds/Z if that is what the host sends), and add negative cases for missing issuedAt, missing seed, wrong field types, invalid timestamps, expired tickets, and expiresAt <= issuedAt. Prove malformed tickets cannot transition READY->RUNNING through either START or RESTART handling. If Godot Time parsing needs a small normalization helper for the host's ISO form, implement that narrowly and test the exact host form.\n3) Align local ticket seed semantics with the repository Phase 3 contract. createPulseLoomRunTicket() currently uses Math.random() when no seed is supplied. Use a deterministic daily seed by default (UTC day is acceptable) while still allowing an explicit seed for tests. New run ticket IDs must remain unique. Add behavioral coverage showing two default tickets on the same day share the deterministic seed while their ticket IDs differ.\n4) Remove the now-inaccurate portal HUD instruction \"LAUNCH: SPACE / CLICK\". Hostless launch was intentionally removed; the visible instructions must tell the player to launch with the portal control/button instead. Do not reintroduce keyboard/touch direct start or retry.\n5) Preserve strictness at the Godot command boundary: settings/ticket data with wrong types must be ignored/rejected rather than broadly coerced. Keep exact event.source and event.origin checks on both sides.\n6) Do not broaden scope. Keep all four Web artifacts required and non-empty; keep generated exports and .ai-factory untracked.\n\nVerification required before PASS: npm run verify; explicit Godot headless smoke covering the complete ticket schema and START/RESTART rejection/acceptance; clean single-threaded Godot 4.6.3 Web export after deleting generated Pulse Loom output; verify index.html/index.js/index.wasm/index.pck are all non-empty; verify production dist contains all four; build the portal with --base=/test/ and prove its dist contains all four under games/pulse-loom; behavioral Node tests for deterministic daily seed plus existing host-ticket persistence, expiry/mismatch, exact-origin, and malformed-payload cases; git status evidence showing generated exports and .ai-factory untracked/ignored. Commit and push the correction to phase/godot-pilot-pulse-loom. Before exit write .ai-factory/result.json with exact commands/outcomes, artifact paths/sizes, git status evidence, and pushed HEAD. Otherwise report FAIL, BLOCKED, or NEEDS_CONTROLLER."
}
