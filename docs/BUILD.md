# Development workflow

Use Node 22+, run `npm install`, then `npm run dev`. Keep each change narrow, update manifests and docs with contract changes, and finish with `npm run verify`. Large game assets should use an external asset pipeline or release storage rather than normal Git history.
